# 凭据模板。复制成 .env 后填真值：
#   cp .env.example .env
#
# .env 已被 .gitignore 忽略，scripts/hooks/pre-commit 还会再拦一道，永远不要提交。
# external_sources.yaml 里只写 ${ENV_VAR} 占位，真值只存在于 .env 和进程环境变量。
#
# dwlib 在解析仓库根目录时自动加载本文件（dwlib.load_dotenv），
# 且**不覆盖**已有的进程环境变量 —— CI 里用真正的 secret 注入即可，无需改代码。

# 示例：某个需要 token 的 http 源
# MY_API_TOKEN=
